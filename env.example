# Environment Variables Example
# Copy this file to .env.local and update the values as needed

# Backend API URLs for json-server
# These should point to the local json-server instances

# Step 1 API (departments, basic info) - runs on port 4001
NEXT_PUBLIC_BASIC_INFO_API=http://localhost:4001

# Step 2 API (locations, details) - runs on port 4002
NEXT_PUBLIC_DETAILS_INFO_API=http://localhost:4002

